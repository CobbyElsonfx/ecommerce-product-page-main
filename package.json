{
  "scripts": {
  "build": "tailwindcss -i ./styles.css -o ./public/output.css",
   "watch": "tailwindcss -i ./styles.css -o ./public/output.css --watch"

},
  "devDependencies": {
    "tailwindcss": "^3.2.7"
  }
}